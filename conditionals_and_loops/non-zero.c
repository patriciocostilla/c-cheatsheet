#include <stdio.h>

int main() {
    int in_stock = 20;
    if (in_stock) { //An expression that evaluates to a non-zero value is considered true.
        printf("Order received.\n");
    }
    return 0;
}